代码见TCP文件夹
/*-----------------------------------TCP编程-------------------------------------*/
#--* 客户端
1.py

#--* 服务器
echo_client.py
echo_server.py

# 小结
用TCP协议进行socket连接十分简单，对于客户端，要主动连接服务器的IP和指定端口；对于服务器，要首先监听指定端口，然后，对每一个新的连接，创建一个线程或进程来处理。通常服务器程序会无限运行下去。

同一个端口，被一个Socket绑定后，就不能被别的Socket绑定了。
